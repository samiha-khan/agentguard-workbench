services:
  - type: web
    name: agentguard
    runtime: docker
    plan: free
    healthCheckPath: /api/evaluations
    envVars:
      - key: JAVA_TOOL_OPTIONS
        value: -Xmx350m -XX:+UseSerialGC
